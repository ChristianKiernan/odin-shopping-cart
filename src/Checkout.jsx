import { Link } from "react-router-dom";

const Checkout = () => {
    return (
        <div id="checkout">
            <h1>Checkout Page</h1>
            <Link to="/">Homepage</Link>
        </div>
    );
};

export default Checkout;
